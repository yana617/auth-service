version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: dockerfile.dev
    volumes:
      - ./:/usr/src/auth-service
      - /usr/src/auth-service/node_modules
    ports:
      - "1081:1081"